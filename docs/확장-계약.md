# 확장 계약 — 새 계산이 받는 것과 내는 것

**생성된 문서다.** `python scripts/describe_extension_api.py` 가 레지스트리에서 만든다 —
손으로 고치지 않는다(아키텍처 시험이 대조한다). 붙이는 방법은
[backend/extensions/README.md](../backend/extensions/README.md), 왜 이렇게 나뉘는지는
[[계획] 계산 확장](%5B계획%5D%20계산%20확장.md).

## 창구 — 전부 `backend/extensions/<이름>/__init__.py` 에서 부른다

| 무엇을 | 등록 함수 | 받는 것 → 내는 것 |
| --- | --- | --- |
| 처리 단계 (시험 하나) | `registry.register(kind="processing")` | `(Frame, options) → StepResult` |
| 묶음 (여러 시험 → 하나) | `registry.register(kind="grouping")` | `(list[Member], **options) → GroupOutcome` |
| 적합식 | `fitting.register_family(Family)` | `evaluate(파라미터, x) → y` |
| 카드 블록 | `cards.register_block(BlockSpec)` | 선언만 — 값·표의 이름과 SI 단위 |
| 솔버 렌더러 | `export.register_renderer(...)` | `(Deck) → Rendered` |

## 채널 — 장비 파일이 주는 열 (내장 시험 종류)

처리 단계의 `Frame.columns` 첫 상태다. 부서가 만든 시험 종류는 이와 다를 수 있다 —
그래서 단계는 키가 아니라 `requires_channels` 로 「이 열이 있으면 된다」 를 선언한다.

| 시험 종류 | 이름 | 채널 (SI) |
| --- | --- | --- |
| `tensile` | 인장시험 | `displacement` (m), `force` (N), `specimen_width` (m) |
| `dma_sweep` | DMA 스윕 | `storage_modulus` (Pa), `loss_modulus` (Pa), `temperature` (K), `angular_frequency` (rad/s), `frequency` (Hz), `tan_delta` (1), `oscillation_strain` (1), `oscillation_stress` (Pa), `step_time` (s), `phase_angle` (rad), `complex_modulus` (Pa) |
| `rheometer_flow` | 레오미터 유동 | `shear_rate` (1/s), `viscosity` (Pa.s), `shear_stress` (Pa), `temperature` (K), `step_time` (s), `normal_stress` (Pa) |
| `fatigue` | 피로 |  |

## 처리 단계 — `Frame` 을 받아 `StepResult` 를 낸다

- `Frame`: `columns: dict[str, ndarray]` + `units: dict[str, str]`. **전부 SI.** 열 이름은 채널 키이거나 앞 단계가 만든 열이다(아래 표).
- `StepResult(frame, scalars, notes)`: 곡선을 바꾸지 않아도 된다. **값을 못 내면 실패하지 않고 `notes` 에 이유를 남긴다** — 뒤 단계가 그 값을 가리키면 그 이유가 오류 문구에 실린다.
- 앞 단계가 낸 스칼라는 옵션에 `"@키"` 로 받는다(`{"proof_stress": "@proof_stress"}`). 레시피는 JSON 으로 저장되므로 참조가 보인다.
- `Scalar(key, label, value, si_unit, dimension=None)`. `Produced.property_key` 를 적으면 문헌 물성 키에 이어진다(값으로 찾기가 잰 값을 찾는다).

### 내장·확장 단계가 만드는 것 (이 이름으로 뒤 단계가 받는다)

| 단계 | 이름 | 만드는 열 | 내는 스칼라 |
| --- | --- | --- | --- |
| `dma.derived` | tan δ · 복소 탄성률 | `tan_delta` (1), `complex_modulus` (Pa), `phase_angle` (rad) | — |
| `tensile.engineering` | 공칭 응력-변형률 | `strain_engineering` (1), `stress_engineering` (Pa) | — |
| `curve.sort_unique` | 정렬·중복 정리 | — | — |
| `dma.frequency` | 주파수 ↔ 각주파수 | `angular_frequency` (rad/s), `frequency` (Hz) | — |
| `dma.lve_modulus` | 선형점탄성 탄성률 (변형률 스윕) | — | `youngs_modulus` (Pa), `lve_strain_limit` (1), `lve_point_count` (1) |
| `dma.to_shear` | 인장 → 전단 (E → G) | `storage_modulus_shear` (Pa), `loss_modulus_shear` (Pa) | — |
| `tensile.toe_compensation` | 토우 보정 | — | `toe_strain_offset` (1), `toe_r_squared` (1) |
| `tensile.yield_drop` | 공칭 하강 처리 | — | `yield_drop_max` (Pa), `yield_drop_points` (1), `event_count` (1), `recovered_count` (1), `partial_count` (1), `unrecovered_count` (1), `boundary_jump_count` (1), `boundary_jump_max` (Pa), `remaining_drop_count` (1), `fit_r_squared` (1), `fit_rmse` (Pa), `model_plateau_stress` (Pa), `model_plateau_start` (1), `model_plateau_end` (1) |
| `curve.crop` | 구간 자르기 | — | — |
| `curve.smooth` | 이동평균 평활 | `{column}_smoothed` (1) | — |
| `tensile.elastic_modulus` | 탄성계수 | — | `youngs_modulus` (Pa), `elastic_intercept` (Pa), `elastic_r_squared` (1), `elastic_slope_reference` (Pa), `elastic_point_count` (1), `elastic_window_start` (1), `elastic_window_end` (1) |
| `dma.glass_transition` | 유리전이온도 | — | `glass_transition` (K), `glass_transition_peak` (1) |
| `tensile.proof_stress` | 오프셋 항복강도 | — | `proof_stress` (Pa), `proof_strain` (1), `proof_offset` (1) |
| `tensile.strength` | 인장강도·연신율 | — | `tensile_strength` (Pa), `strain_at_strength` (1), `elongation_observed` (1) |
| `tensile.necking_candidate` | 네킹 후보 | — | `necking_candidate_index` (1), `necking_candidate_strain` (1), `necking_candidate_stress` (Pa) |
| `tensile.yield_ratio` | 항복비 | — | `yield_ratio` (1) |
| `tensile.true_plastic` | 진응력·진소성변형률 | `strain_true` (1), `stress_true` (Pa), `strain_true_plastic` (1) | — |
| `curve.monotone` | 단조 증가 보정 | — | `monotone_points` (1), `monotone_max_lift` (1) |
| `curve.resample` | 균등 격자로 재샘플 | — | — |

## 묶음 — `Member` 들을 받아 `GroupOutcome` 을 낸다

- `Member(label, columns, values)`: 시험 하나에서 꺼낸 곡선과 스칼라. 둘 이상이어야 묶인다.
- `GroupOutcome(values, columns, detail, warnings, used)`: 처리 결과와 같은 모양(곡선 하나 + 스칼라). `used` 는 실제로 쓴 구성원.
- **구성원을 모으는 법은 선언한다** — `register(..., members={"from": "adopted_result", "columns": [...], "conditions": [...], "values": [...]})`. 채택된 처리 결과의 곡선 열·시험 조건·스칼라를 그 이름으로 꺼내 준다. 계산이 필요한 것(변형률 속도)은 중심 코드의 `@collector` 다.

| 묶음 | 이름 | 구성원 | 내는 스칼라 |
| --- | --- | --- | --- |
| `viscoelastic.prony_group` | Prony 글로벌 피팅 | 중심 코드의 수집기 | `equilibrium_pa` (Pa), `instantaneous_pa` (Pa), `reference_temperature_k` (K), `term_count` (1), `normalized_rmse` (1) |
| `tensile.rate_family` | 속도별 소성 곡선 | 중심 코드의 수집기 | `rate_count` (1), `reference_rate` (1/s), `rate_min` (1/s), `rate_max` (1/s), `cs_d` (1/s), `cs_p` (1), `jc_c` (1), `model_r_squared` (1) |
| `tensile.temperature_family` | 온도별 소성 곡선 | 선언: 열 `strain_true_plastic`, `stress_true` · 조건 `temperature` | `temperature_count` (1), `reference_temperature` (K), `temperature_min` (K), `temperature_max` (K), `softening_slope` (Pa/K), `softening_r_squared` (1), `jc_m` (1), `model_r_squared` (1) |
| `fatigue.sn_curve` | S-N 곡선 (Basquin) | 선언: 열  · 조건 `stress_amplitude`, `stress_ratio`, `frequency` | `basquin_a` (Pa), `basquin_b` (1), `sn_r_squared` (1), `point_count` (1), `runout_count` (1), `cycles_min` (1), `cycles_max` (1), `log_scatter` (1), `strength_at_1e5` (Pa), `strength_at_1e6` (Pa), `strength_at_1e7` (Pa) |

## 적합식 — `Family`

- `evaluate(parameters, x) → y` · `guess(x, y) → 초기값` · `bounds(x, y) → (하한, 상한)`. 축은 `x_column`·`y_column` 으로 선언한다(위 「만드는 열」 의 이름).
- 결과는 `block` 이 가리키는 카드 블록에 담긴다. `applies_to` 로 재료군을 가른다 — 금속 경화식과 고무 초탄성을 한 줄로 세우지 않는다.

| 식 | 이름 | 축 (x → y) | 파라미터 (SI) | 블록 | 재료군 |
| --- | --- | --- | --- | --- | --- |
| `carreau` | Carreau | `shear_rate` → `viscosity` | `eta_0` (Pa.s), `eta_inf` (Pa.s), `lambda` (s), `n` (1) | `rheology` | Polymer, Plastic, Fluid, Adhesive, Ink, Slurry |
| `cross` | Cross | `shear_rate` → `viscosity` | `eta_0` (Pa.s), `eta_inf` (Pa.s), `lambda` (s), `m` (1) | `rheology` | Polymer, Plastic, Fluid, Adhesive, Ink, Slurry |
| `ghosh` | Ghosh | `strain_true_plastic` → `stress_true` | `k` (Pa), `epsilon_0` (1), `n` (1), `p` (Pa) | `hardening` | Metal |
| `hockett_sherby` | Hockett-Sherby | `strain_true_plastic` → `stress_true` | `sigma_0` (Pa), `q` (Pa), `b` (1), `n` (1) | `hardening` | Metal |
| `johnson_cook_static` | Johnson-Cook (준정적 항) | `strain_true_plastic` → `stress_true` | `a` (Pa), `b` (Pa), `n` (1) | `hardening` | Metal |
| `mooney_rivlin` | Mooney-Rivlin | `strain_engineering` → `stress_engineering` | `c10` (Pa), `c01` (Pa) | `hyperelastic` | Rubber, Elastomer, Polymer |
| `neo_hookean` | Neo-Hookean | `strain_engineering` → `stress_engineering` | `c10` (Pa) | `hyperelastic` | Rubber, Elastomer, Polymer |
| `ogden_1` | Ogden (1항) | `strain_engineering` → `stress_engineering` | `mu` (Pa), `alpha` (1) | `hyperelastic` | Rubber, Elastomer, Polymer |
| `swift` | Swift | `strain_true_plastic` → `stress_true` | `k` (Pa), `epsilon_0` (1), `n` (1) | `hardening` | Metal |
| `voce` | Voce | `strain_true_plastic` → `stress_true` | `sigma_0` (Pa), `q` (Pa), `b` (1) | `hardening` | Metal |
| `yeoh` | Yeoh | `strain_engineering` → `stress_engineering` | `c10` (Pa), `c20` (Pa), `c30` (Pa) | `hyperelastic` | Rubber, Elastomer, Polymer |

## 카드 블록 — `BlockSpec`

값(`produces`)과 표 열(`rows`)의 이름·SI 단위를 선언한다. 화면·저장·덱이 이 선언만 읽는다.
블록이 드는 단위는 `matcore/export/systems.py` 의 단위계가 알아야 한다 — 새 단위를 들면 `DECLARED` 와 두 계의 기호에 함께 적는다(시험이 대조한다).

| 블록 | 이름 | 값 | 표 열 | 종류 우선순위 |
| --- | --- | --- | --- | --- |
| `elastic` | 탄성 | `youngs_modulus` (Pa), `poisson_ratio` (1), `density` (kg/m3) | `temperature` (K), `youngs_modulus` (Pa), `poisson_ratio` (1) | — |
| `thermal` | 열물성 | `thermal_expansion` (1/K), `specific_heat` (J/(kg.K)), `thermal_conductivity` (W/(m.K)), `reference_temperature` (K) | `temperature` (K), `thermal_expansion` (1/K), `specific_heat` (J/(kg.K)), `thermal_conductivity` (W/(m.K)) | 6 |
| `hardening` | 경화식 | `label` (1), `relative_rmse` (1), `r_squared` (1), `max_residual` (Pa), `strain_min` (1), `strain_max` (1) | `name` (1), `value` (1) | 3 |
| `hyperelastic` | 초탄성 | `label` (1), `mode` (1), `shear_modulus` (Pa), `relative_rmse` (1), `r_squared` (1), `max_residual` (Pa), `strain_min` (1), `strain_max` (1) | `name` (1), `value` (1) | 2 |
| `table` | 소성 표 | `source` (1), `measured_max` (1), `extrapolated_to` (1), `family` (1), `junction_gap` (1) | `plastic_strain` (1), `true_stress` (Pa) | — |
| `rate_table` | 속도별 소성 표 | `source` (1), `rate_count` (1), `reference_rate` (1/s), `model` (1), `cs_d` (1/s), `cs_p` (1), `jc_c` (1), `model_r_squared` (1) | `strain_rate` (1/s), `plastic_strain` (1), `true_stress` (Pa) | 1 |
| `temperature_table` | 온도별 소성 표 | `source` (1), `temperature_count` (1), `reference_temperature` (K), `model` (1), `melt_temperature` (K), `jc_m` (1), `model_r_squared` (1) | `temperature` (K), `plastic_strain` (1), `true_stress` (Pa) | 1 |
| `rheology` | 유변 | `label` (1), `zero_shear_viscosity` (Pa.s), `onset_shear_rate` (1/s), `relative_rmse` (1), `r_squared` (1), `max_residual` (Pa.s), `strain_min` (1/s), `strain_max` (1/s) | `name` (1), `value` (1) | 2 |
| `viscoelastic` | 점탄성 | `equilibrium_pa` (Pa), `instantaneous_pa` (Pa), `reference_temperature_k` (K), `normalized_rmse` (1), `bic` (1), `shift_method` (1) | `relaxation_time_s` (s), `modulus_pa` (Pa), `relative_modulus` (1) | 4 |
| `lve` | 선형탄성구간(LVE) 탄성률 | `youngs_modulus` (Pa), `lve_strain_limit` (1), `sample_count` (1), `coefficient_of_variation` (1), `frequency_hz` (Hz), `temperature_k` (K) | — | 5 |
| `sn_curve` | S-N 곡선 | `source` (1), `model` (1), `basquin_a` (Pa), `basquin_b` (1), `sn_r_squared` (1), `point_count` (1), `runout_count` (1), `log_scatter` (1), `strength_at_1e5` (Pa), `strength_at_1e6` (Pa), `strength_at_1e7` (Pa) | `stress_amplitude` (Pa), `cycles_to_failure` (1), `runout` (1) | 3 |
| `model_params` | 모델 파라미터 | `sets` (1), `models` (1) | `set` (1), `name` (1), `value` (1), `unit` (1) | — |

선언 단위(`DECLARED`): `1`, `Pa`, `K`, `s`, `kg/m3`, `1/K`, `J/(kg.K)`, `W/(m.K)`, `1/s`, `Hz`, `Pa.s`

## 솔버 렌더러 — `Renderer`

`needs` 가 먹는 블록과 값을 말한다. 없으면 그 솔버로는 못 내고, 화면이 그 이유를 말한다.

| 렌더러 | 이름 | 확장자 | 먹는 블록 |
| --- | --- | --- | --- |
| `abaqus` | Abaqus | .inp | `elastic`[youngs_modulus, poisson_ratio], `elastic`[density] (선택), `thermal` (선택), `table` |
| `abaqus_hyperelastic` | Abaqus (초탄성) | .inp | `hyperelastic`, `elastic`[density] (선택), `thermal` (선택) |
| `abaqus_lve` | Abaqus (선형탄성구간 · DMA) | .inp | `elastic`[youngs_modulus, poisson_ratio], `elastic`[density] (선택), `thermal` (선택), `lve`[youngs_modulus, lve_strain_limit] |
| `abaqus_rate` | Abaqus (속도 의존) | .inp | `elastic`[youngs_modulus, poisson_ratio], `elastic`[density] (선택), `thermal` (선택), `rate_table`[rate_count] |
| `abaqus_temperature` | Abaqus (온도 의존) | .inp | `elastic`[youngs_modulus, poisson_ratio], `elastic`[density] (선택), `thermal` (선택), `temperature_table`[temperature_count] |
| `abaqus_viscoelastic` | Abaqus (점탄성) | .inp | `elastic`[youngs_modulus, poisson_ratio], `thermal` (선택), `viscoelastic` |
| `abaqus_viscosity` | Abaqus (점도) | .inp | `rheology`[label], `elastic`[density] (선택) |
| `dyna` | LS-DYNA | .k | `elastic`[youngs_modulus, poisson_ratio, density], `table` |
| `dyna_elastic` | LS-DYNA (탄성) | .k | `elastic`[youngs_modulus, poisson_ratio, density] |
| `dyna_johnson_cook` | LS-DYNA (Johnson-Cook 단순화) | .k | `elastic`[youngs_modulus, poisson_ratio, density], `hardening`, `rate_table` (선택) |
| `dyna_thermal` | LS-DYNA (열물성) | .k | `thermal`[specific_heat, thermal_conductivity], `elastic` (선택) |
| `dyna_viscoelastic` | LS-DYNA (점탄성) | .k | `elastic`[youngs_modulus, poisson_ratio, density], `viscoelastic` |
| `json` | 중립 JSON | .json | — |
| `openradioss` | OpenRadioss | .rad | `elastic`[youngs_modulus, poisson_ratio, density], `table` |
| `openradioss_thermal` | OpenRadioss (열물성) | .rad | `elastic`[density], `thermal`[specific_heat] |

## 지금 붙어 있는 확장 (`backend/extensions/`)

| 폴더 | 상태 |
| --- | --- |
| `fatigue` | 읽힘 |
| `ghosh_hardening` | 읽힘 |
| `johnson_cook_static` | 읽힘 |
| `rheology` | 읽힘 |
| `tensile_extras` | 읽힘 |
